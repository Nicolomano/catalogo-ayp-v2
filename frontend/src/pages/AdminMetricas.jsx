import AdminMetrics from "../components/AdminMetrics.jsx";

/** Página propia de métricas. Antes vivía dentro del Panel, entre las tarjetas
 *  de productos y el listado, donde pasaba desapercibida. */
export default function AdminMetricas() {
  return <AdminMetrics />;
}
