# Valores de producción (Vite carga este archivo solo al buildear en modo production).
# Se usa para los links que se comparten y el canonical de Google.
# Va con www: el apex redirige 307 a www.
# VITE_API_URL no se define acá a propósito: sale del panel de Vercel.

VITE_SITE_URL=https://www.refrigeracionayp.com
